\section{Incompressible Navier–Stokes Equations}
\label{sec:cylinder-nonlinear-dwr}

This final experiment extends the automated space--time framework to a nonlinear mixed problem: incompressible Navier--Stokes flow past a cylinder. 
The benchmark geometry is taken from \cite{benchmark,reference_value}, while the tensor-product space--time discretisation follows Roth et al.~\cite{non-flow}. More specifically, we use discontinuous Galerkin discretisation in time, Taylor--Hood finite elements \cite{TaylorHood} for the velocity--pressure pair, and a divergence-free projection \cite{projection} to transfer the velocity between successive spatial meshes. The automated bubble-recovery localisation and the resulting local error indicators are those developed in this thesis.


\subsection{Problem and Quantity of Interest}
\label{subsec:cylinder-problem}

Let
\(
  \Omega=((0,2.2)\times(0,0.41))\setminus\overline{B_{0.05}(0.2,0.2)}
\)
be the channel-with-cylinder domain, $I=(0,T)$, and take $T=8$ and viscosity \(\nu=10^{-3}\). The boundary is decomposed into the inlet \(\Gamma_{\mathrm{in}}\), channel walls and cylinder \(\Gamma_{\mathrm{wall}}\cup\Gamma_C\), and outlet \(\Gamma_{\mathrm{out}}\). We solve velocity and pressure \(U=(\boldsymbol u,p)\) satisfying
\begin{subequations}
\label{eq:cylinder-strong}
\begin{align}
  \partial_t\boldsymbol u+
  (\boldsymbol u\!\cdot\!\nabla)\boldsymbol u
  -\nu\Delta\boldsymbol u+\nabla p &= \boldsymbol 0
  &&\text{in }\Omega\times I,\label{eq:cylinder-momentum}\\
  \nabla\!\cdot\boldsymbol u &=0
  &&\text{in }\Omega\times I.\label{eq:cylinder-continuity}
\end{align}
\end{subequations}
The prescribed inflow is
\begin{equation}
 \boldsymbol u_{\mathrm{in}}(y,t)
 =\left(
  6\sin(\pi t/8)\frac{y(0.41-y)}{0.41^2},0
 \right)^{T},
 \label{eq:cylinder-inflow}
\end{equation}
with no slip on \(\Gamma_{\mathrm{wall}}\cup\Gamma_C\) and the natural traction condition at the outlet.  This is the nonstationary cylinder benchmark used by Roth et al.~\cite{non-flow}.

For
\[
 V_g(t)=\{\boldsymbol v\in[H^1(\Omega)]^2:
 \boldsymbol v=\boldsymbol g(t)\text{ on the essential boundary}\},
 \qquad Q=L^2_0(\Omega),
\]
\pef{If you have an outflow boundary condition, the pressure space should be $Q = L^2(\Omega)$, not $Q = L^2_0(\Omega)$ (since the natural outflow boundary condition involves the value of the pressure on the boundary, the pressure is no longer undefined up to a constant). Double check your code to make sure you're not imposing the mean-zero constraint, it shouldn't be.}
the weak spatial operator is
\begin{align}
 a(U;\Phi)={}&
 ((\boldsymbol u\!\cdot\!\nabla)\boldsymbol u,\boldsymbol v)_\Omega
 +\nu(\nabla\boldsymbol u,\nabla\boldsymbol v)_\Omega
 -(p,\nabla\!\cdot\boldsymbol v)_\Omega
  +(\nabla\!\cdot\boldsymbol u,q)_\Omega,
 \label{eq:cylinder-spatial-operator}
\end{align}
where \(\Phi=(\boldsymbol v,q)\in V_0\times Q\). The complete weak problem adds \((\partial_t\boldsymbol u,\boldsymbol v)_\Omega\) and the initial-condition term.

The quantity of interest is the time-mean drag on the cylinder,
\begin{equation}
 J(U)=\frac1T\int_0^T C_D(U(t))\,\mathrm dt,
 \qquad
 C_D(U)=20\int_{\Gamma_C}
 \boldsymbol e_x\!\cdot\!\bigl((-pI+\nu\nabla\boldsymbol u)
 \boldsymbol n_C\bigr)\,\mathrm ds,
 \label{eq:cylinder-goal}
\end{equation}
where \(\boldsymbol n_C\) points from the cylinder into the fluid. We use the reference value \(J_{\mathrm{ref}}=1.6031368\), which is reported in \cite{non-flow}.
% \pef{From Roth?}
% \yue{reference updated}


\subsection{Implementation}
\label{subsec:cylinder-mixed-treatment}

For the problem, pressure is a Lagrange multiplier for incompressibility. The semidiscrete system is a differential--algebraic equation with singular mixed mass operator
\begin{equation}
 \mathcal M=
 \begin{pmatrix}M_u&0\\0&0\end{pmatrix}.
 \label{eq:cylinder-dae-mass}
\end{equation}
Only velocity is propagated between time slabs.
Following the mixed-order setting of~\cite{non-flow}, on each slab \(I_n=(t_{n-1},t_n)\) the primal spatial pair is the inf--sup stable continuous-Galerkin Taylor--Hood element. The velocity and pressure space can be denoted as 
\begin{equation}
 V_{h,n}=[\mathrm{cG}(2;\mathcal T_n)]^2,
 \qquad Q_{h,n}=\mathrm{cG}(1;\mathcal T_n)\cap L^2_0(\Omega),
 \label{eq:cylinder-taylor-hood}
\end{equation}
\pef{Shouldn't have intersection with $L^2_0(\Omega)$}
and the primal uses \(\mathrm{dG}(1)\) in time. Velocity and pressure are solved together. The resulting velocity is discretely incompressible:
\begin{equation}
 (\nabla\!\cdot\boldsymbol u_h,q_h)_\Omega=0
 \qquad\forall q_h\in Q_{h,n},
 \label{eq:cylinder-discrete-divergence}
\end{equation}
but not exactly incompressible.

The enriched spaces used for the high-order adjoint is \(([\mathrm{cG}3]^2 \times \mathrm{cG}2) \otimes \mathrm{dG}(2)\),
% \pef{The natural enrichment would be to increase both polynomial degrees (for velocity and pressure) by one. The important point is that a differential operator applied to velocity maps you to a pressure, so their difference being one is the important thing, not the ratio between them.}
\begin{equation}
 V^+_{h,n}=[\mathrm{cG}(3;\mathcal T_n)]^2,
 \qquad Q^+_{h,n}=\mathrm{cG}(2;\mathcal T_n)\cap L^2_0(\Omega),
 \qquad \mathrm{dG}(2)\text{ in time}.
 \label{eq:cylinder-enriched-space}
\end{equation}
\pef{Shouldn't have intersection with $L^2_0(\Omega)$}

From our slabwise refinement strategy, spatial meshes may change from one slab to the next. A direct nodal interpolation of the outgoing velocity generally does not preserve the discretely divergence-free subspace associated with the receiving Taylor--Hood pair. Following the divergence-free transfer strategy described in \cite{projection} and its implementation in \cite{non-flow}, we define the transfer between consecutive time slabs as follows. Let
\[
V_{h,n,0}
:=
\left\{
\boldsymbol v_h\in V_{h,n}:
\boldsymbol v_h=\boldsymbol 0
\text{ on }\Gamma_D
\right\},
\]
and let \(V_{h,n,g}\) denote the affine velocity space satisfying the prescribed Dirichlet data \(g\). We first transfer the outgoing velocity trace to the new mesh by defining
\[
\boldsymbol{s}_n:=I_n\boldsymbol{u}^-,
\qquad
I_n:V_{h,n-1}\to V_{h,n}.
\]
The interpolated velocity \(\boldsymbol{s}_n\) is then projected onto the discretely divergence-free subset of \(V_{h,n,g}\). Specifically, we find
\[
\bigl(P_n\boldsymbol{u}^-,\lambda_n\bigr)
\in V_{h,n,g}\times Q_{h,n}
\]
such that
\begin{subequations}
\begin{align}
\bigl(P_n\boldsymbol{u}^-,\boldsymbol v_n\bigr)_\Omega
-
\bigl(\lambda_n,\nabla\cdot\boldsymbol v_n\bigr)_\Omega
&=
\bigl(\boldsymbol{s}_n,\boldsymbol v_n\bigr)_\Omega
&&
\forall \boldsymbol v_n\in V_{h,n,0},\\
\bigl(\nabla\cdot P_n\boldsymbol{u}^-,q_n\bigr)_\Omega
&=0
&&
\forall q_n\in Q_{h,n}.
\end{align}
\end{subequations}
Here, \(\lambda_n\) is the Lagrange multiplier associated with the discrete divergence constraint. Let \(Q_{n,\mathrm{df}}^{L^2}\) denote the constrained \(L^2\)-projection onto the discretely divergence-free target set. When this projection is activated, the complete transfer is defined by
\begin{equation}
P_n\boldsymbol{u}^-
:=
Q_{n,\mathrm{df}}^{L^2}
\bigl(I_n\boldsymbol{u}^-\bigr).
\end{equation}
% Equivalently, at the operator level,
% \[
% P_n
% =
% Q_{n,\mathrm{df}}^{L^2}\circ I_n.
% \]

Although the primal Navier--Stokes problem is nonlinear, we use the economical one-term primal-residual estimator
\begin{equation}
  \eta := \rho(U_h)\bigl(Z_h^+-I_hI_kZ_h^+\bigr),
  \label{eq:cylinder-linear-estimator}
\end{equation}
where \(Z_h^+\) is the enriched adjoint. Here \(I_k\) is the Gauss--Legendre nodal interpolant from \(\mathrm{dG}(2)\) to \(\mathrm{dG}(1)\) on each time slab, and \(I_h\) is the componentwise nodal interpolant from the enriched \([\mathrm{cG}(3)]^2 \times \mathrm{cG}(2)\) pair to the primal \([\mathrm{cG}(2)]^2\times \mathrm{cG}(1)\) pair. This choice avoids the low-order adjoint and enriched-primal solves required by the nonlinear estimator. It is also motivated by the asymptotic equivalence of the primal- and adjoint-residual terms for sufficiently smooth, mildly nonlinear semilinear problems with a linear goal functional \cite{non-flow}. 


\subsection{Initial Discretisation and Adaptive Strategy}
\label{subsec:cylinder-adaptive-configuration}

The initial spatial mesh is the first uniformly refined level of the geometry-fitted triangular \texttt{Netgen} mesh. It contains \(1092\) triangles and \(5239\) mixed Taylor--Hood degrees of freedom on each time slab. We start with \(N_t=40\) uniform time slabs, so that \(\Delta t=0.2\). This is a compromise between the cost of the initial solves and the need to resolve the shorter time scales generated in the wake, i.e. the downstream flow region behind the cylinder, as vortex shedding develops.

Both our preliminary experiments and the reference computations indicate that substantial temporal refinement is needed before the estimator enters a useful regime. Roth et al.~\cite{non-flow} bisect the \(75\%\) of time slabs with the largest temporal indicators in each adaptive loop, producing the sequence
\[
  20,\;36,\;64,\;113,\;199.
\]
\pef{What does this sequence mean? Is this the number of cells in time? Not clear}
The more recent computation \cite{recent-flow} instead starts from \(160\) time slabs and subsequently applies an approximately \(10\%\) fixed-rate temporal refinement.

Motivated by these observations, we use a two-stage temporal strategy. First, the score of time slab \(I_n\) is obtained directly from the already localised strong-residual indicators,
\[
  E_n := \sum_{K\in\mathcal{T}_h^n}\lvert\eta_{K,n}\rvert.
\]
The \(75\%\) of slabs with the largest \(E_n\) are bisected. Two refinements give
\[
  40\longrightarrow70\longrightarrow123.
\]
Thereafter, we return to the cell-fraction trigger: slab \(I_n\) is eligible for bisection when more than \(1\%\) of its spatial cells have been selected by the spatial marking. At most
\[
  \min(0.30N_t,20)
\]
slabs are bisected in one loop. Spatial cells are still selected by global D\"orfler marking with \(\theta_h=0.40\) and are refined independently on each slab. This combination produces the time-grid sequence
\[
  40,\;70,\;123,\;143,\;163,\;183,\;203.
\]


\subsection{Results and Interpretation}
\label{subsec:cylinder-results}

\Cref{tab:cylinder-adaptive-results} reports the resulting adaptive sequence. The mean drag increases monotonically from \(1.534362\) to \(1.598684\), while the error relative to \(J_{\mathrm{ref}}=1.6031368\) decreases from \(6.88\times10^{-2}\) to \(4.45\times10^{-3}\), a reduction of approximately \(93.5\%\). The estimator has the correct sign in every iteration, and the localisation gap is negligible.

\begin{table}[htbp]
\centering
\caption{Adaptive Navier--Stokes computation.}
\label{tab:cylinder-adaptive-results}
\begin{tabular}{rrrrrrr}
\toprule
\(\ell\) & \(N_t\) & primal DoFs & \(J(U_h)\) & \(\lvert J_{\mathrm{ref}}-J(U_h)\rvert\) & \(\eta\) & \(I_{\mathrm{eff}}\)\\
\midrule
0 &  40 &   419120 & 1.534362 & \(6.8774\times10^{-2}\) & \(4.4338\times10^{-2}\) & 0.645\\
1 &  70 &   756508 & 1.548852 & \(5.4285\times10^{-2}\) & \(2.6750\times10^{-1}\) & 4.928\\
2 & 123 &  1442810 & 1.554548 & \(4.8588\times10^{-2}\) & \(9.2346\times10^{-2}\) & 1.901\\
3 & 143 &  2036664 & 1.561633 & \(4.1503\times10^{-2}\) & \(7.2375\times10^{-2}\) & 1.744\\
4 & 163 &  2836230 & 1.572898 & \(3.0239\times10^{-2}\) & \(3.8520\times10^{-2}\) & 1.274\\
5 & 183 &  4105792 & 1.592390 & \(1.0746\times10^{-2}\) & \(1.5197\times10^{-2}\) & 1.414\\
6 & 203 &  6081534 & 1.598684 & \(4.4532\times10^{-3}\) & \(7.1917\times10^{-3}\) & 1.615\\
\bottomrule
\end{tabular}
\end{table}

The goal error therefore converges convincingly, but the effectivity index does not yet exhibit asymptotic stabilisation. It decreases from a strong overestimate at iteration 1 to \(1.274\) at iteration 4 and then rises to \(1.615\). In particular, a single value close to one should not be interpreted as convergence of the estimator; stability must be assessed over several consecutive refinements.

The adaptive result is substantially more accurate than the available uniform space--time refinement sequence.  The latter gives an error of \(2.20\times10^{-2}\) with \(1.279\times10^7\) primal space--time degrees of freedom, whereas the present adaptive computation gives \(4.45\times10^{-3}\) with \(6.082\times10^6\) degrees of freedom. Thus, in this comparison, adaptivity obtains approximately one fifth of the uniform error with less than one half of the primal degrees of freedom. However, note that this comparison concerns goal accuracy rather than running time. The essential trade-off is between avoiding unnecessary refinement throughout the space--time domain and paying the additional cost of the enriched solve, which constitutes the main overhead of the adaptive method. When adaptivity reduces the goal error to a level that would be prohibitively expensive for uniform refinement to attain, this additional cost can be regarded as justified.

The final temporal mesh is shown in Figure~\ref{fig:cylinder-temporal-mesh}. It contains \(203\) time slabs, with \(\Delta t_{\min}=0.00625\) and \(\Delta t_{\max}=0.2\). Most temporal refinement is concentrated during the development of the time-dependent wake, whereas the final part of the interval remains relatively coarse.
\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/flow/ns_time.png}
  \caption{Temporal mesh in adaptive iteration 6.}
  \label{fig:cylinder-temporal-mesh}
\end{figure}



Finally, ~\cref{fig:cylinder-velocity-snapshots} shows the velocity magnitude on the slabwise spatial meshes in the final adaptive loop. At \(t=3\), the flow remains approximately symmetric in the near wake. By \(t=5\), the asymmetric velocity pattern associated with the developing vortex street is clearly visible downstream of the cylinder. The snapshot at \(t=6\) shows the subsequent evolution of this structure. The mesh is concentrated near the cylinder and in the dynamically active part, while remaining comparatively coarse farther downstream.
\begin{figure}[htbp]
  \centering

  \begin{subfigure}{0.9\textwidth}
    \centering
    \includegraphics[width=\linewidth]
      {figures/flow/velocity-t3.png}
    \caption{$t=3$}
  \end{subfigure}

  \vspace{0.5em}

  \begin{subfigure}{0.9\textwidth}
    \centering
    \includegraphics[width=\linewidth]
      {figures/flow/velocity-t5.png}
    \caption{$t=5$}
  \end{subfigure}

  \vspace{0.5em}

  \begin{subfigure}{0.9\textwidth}
    \centering
    \includegraphics[width=\linewidth]
      {figures/flow/velocity-t6.png}
    \caption{$t=6$}
  \end{subfigure}

  \vspace{0.6em}

  % Shared colour bar — intentionally no subcaption
  \includegraphics[width=0.80\textwidth]
    {figures/flow/velocity-magnitude-colorbar.png}

  \caption{Snapshots of the velocity magnitude and refined meshes of the primal Navier--Stokes solution at \(t=3\), \(5\), and \(6\) in adaptive iteration 6.}
  \label{fig:cylinder-velocity-snapshots}
\end{figure}




